# TOWER Ads SDK v3.0 - Интеграция

## 🔐 Безопасная архитектура

**Вся бизнес-логика на сервере!**
- ✅ Валидация ключей
- ✅ Проверка домена
- ✅ Frequency cap
- ✅ Выбор сети из waterfall
- ✅ Защита от накрутки

SDK только показывает рекламу и отправляет события.

---

## 📦 Установка

### Вариант 1: CDN
```html
<script src="https://cdn.usl.app/sdk/v3/tower-ads.js"></script>
```

### Вариант 2: NPM
```bash
npm install @tower-ads/sdk
```

---

## 🚀 Быстрый старт

### 1. Получите App ID
Зарегистрируйтесь на [usl.app](https://usl.app) и получите публичный `appId` для вашего приложения.

### 2. Добавьте SDK
```html
<!DOCTYPE html>
<html>
<head>
  <title>My App</title>
</head>
<body>
  <button id="show-ad-btn">Показать рекламу</button>

  <!-- TOWER Ads SDK -->
  <script src="https://cdn.usl.app/sdk/v3/tower-ads.js"></script>
  <script>
    const ads = new TowerAds({
      appId: 'app_xxxxx',  // Ваш App ID
      
      // Callbacks
      onAdLoaded: (network) => {
        console.log('Реклама загружена из:', network);
      },
      onAdShown: (network) => {
        console.log('Реклама показана:', network);
      },
      onRewardEarned: (reward) => {
        console.log('Награда получена:', reward);
        // Начислите награду пользователю
        giveUserCoins(reward.amount);
      },
      onError: (error) => {
        console.error('Ошибка рекламы:', error);
      }
    });

    // Показать рекламу при клике
    document.getElementById('show-ad-btn').onclick = () => {
      ads.show('main_banner');
    };
  </script>
</body>
</html>
```

---

## 📋 API Reference

### Инициализация

```javascript
const ads = new TowerAds({
  appId: 'app_xxxxx',           // Обязательно: ваш App ID
  apiUrl: 'https://api.usl.app/v1',  // Опционально: URL API
  
  // Callbacks
  onAdLoaded: (network) => {},   // Реклама загружена
  onAdShown: (network) => {},    // Реклама показана
  onAdClosed: () => {},          // Реклама закрыта
  onAdClicked: () => {},         // Клик по рекламе
  onRewardEarned: (reward) => {},// Награда получена
  onError: (error) => {}         // Ошибка
});
```

### Методы

#### `ads.load(placement)`
Предзагрузка рекламы (опционально).

```javascript
await ads.load('main_banner');
```

#### `ads.show(placement)`
Показать рекламу. Автоматически загружает, если не загружена.

```javascript
await ads.show('main_banner');
```

**Placement** - название места размещения:
- `'main_banner'` - главный баннер
- `'rewarded_video'` - видео с наградой
- `'interstitial'` - полноэкранная реклама

---

## 🎯 Типы рекламы

### 1. Rewarded Video (Видео с наградой)
```javascript
ads.show('rewarded_video');

// Callback
onRewardEarned: (reward) => {
  console.log('Награда:', reward);
  // reward = { type: 'coins', amount: 100 }
  giveUserCoins(reward.amount);
}
```

**Особенности:**
- 5-секундный таймер перед кнопкой "Закрыть"
- Награда начисляется только при полном просмотре
- Автоматическое подтверждение на сервере

### 2. Interstitial (Полноэкранная)
```javascript
ads.show('interstitial');
```

**Особенности:**
- Полноэкранное изображение
- Кнопка закрытия в углу
- Клик по изображению открывает рекламодателя

### 3. Banner (Баннер)
```javascript
// Добавьте контейнер в HTML
<div id="tower-ad-banner"></div>

// Покажите баннер
ads.show('banner');
```

**Особенности:**
- Встраивается в указанный контейнер
- Адаптивный размер
- Минимальное влияние на UX

---

## 🔒 Безопасность

### Что делает сервер:

1. **Валидация App ID**
   - Проверяет, что App ID существует и активен

2. **Проверка домена**
   - Разрешены только домены из whitelist
   - Защита от кражи App ID

3. **Frequency Cap**
   - Ограничение показов на пользователя
   - Защита от накрутки

4. **Выбор сети (Waterfall)**
   - Сервер выбирает оптимальную сеть
   - Клиент не может обойти медиацию

5. **Одноразовые токены**
   - Каждый показ подтверждается уникальным токеном
   - Невозможно подделать показы

### Что делает SDK:

1. **Только UI**
   - Показывает рекламу
   - Обрабатывает взаимодействия

2. **Отправка событий**
   - Подтверждение показа
   - Трекинг кликов
   - Отчет о завершении

3. **Никаких секретов**
   - Нет API ключей
   - Нет настроек сетей
   - Только публичный App ID

---

## 📊 Аналитика

Все события автоматически отправляются на сервер:

- **Impressions** - показы рекламы
- **Clicks** - клики по рекламе
- **Completions** - завершенные просмотры
- **Rewards** - выданные награды

Смотрите статистику в [личном кабинете](https://usl.app/dashboard).

---

## 🎨 Кастомизация

### Изменение стилей

SDK использует inline-стили, но вы можете переопределить их:

```javascript
const ads = new TowerAds({
  appId: 'app_xxxxx',
  styles: {
    overlay: {
      background: 'rgba(0, 0, 0, 0.98)'
    },
    closeButton: {
      background: '#ff4444',
      borderRadius: '8px'
    }
  }
});
```

---

## ⚡ Best Practices

### 1. Предзагрузка
```javascript
// Загрузите рекламу заранее
await ads.load('rewarded_video');

// Покажите мгновенно
document.getElementById('btn').onclick = () => {
  ads.show('rewarded_video');
};
```

### 2. Обработка ошибок
```javascript
const ads = new TowerAds({
  appId: 'app_xxxxx',
  onError: (error) => {
    console.error('Ad error:', error);
    // Покажите альтернативный контент
    showFallbackContent();
  }
});
```

### 3. Частота показов
```javascript
// Не показывайте рекламу слишком часто
let lastAdTime = 0;
const MIN_INTERVAL = 60000; // 1 минута

function showAd() {
  const now = Date.now();
  if (now - lastAdTime < MIN_INTERVAL) {
    console.log('Слишком рано для новой рекламы');
    return;
  }
  
  ads.show('interstitial');
  lastAdTime = now;
}
```

---

## 🐛 Отладка

### Включить debug режим
```javascript
const ads = new TowerAds({
  appId: 'app_xxxxx',
  debug: true  // Выводит логи в консоль
});
```

### Проверка загрузки
```javascript
const loaded = await ads.load('main_banner');
if (loaded) {
  console.log('Реклама загружена успешно');
} else {
  console.log('Не удалось загрузить рекламу');
}
```

---

## 📞 Поддержка

- **Документация**: [docs.usl.app](https://docs.usl.app)
- **Email**: support@usl.app
- **Telegram**: [@usl_support](https://t.me/usl_support)

---

## 📄 Лицензия

MIT License - используйте свободно в коммерческих и некоммерческих проектах.
